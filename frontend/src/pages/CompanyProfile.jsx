import PlaceholderPage from '../components/PlaceholderPage'

export default function CompanyProfile() {
  return (
    <PlaceholderPage
      title="Company Profile"
      phase="soon"
      description="Logo, name, address, phone, email, GST, and bank details — used as the fixed header on every quotation."
    />
  )
}
